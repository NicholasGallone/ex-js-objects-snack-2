1 console.log(hamburger.name); // 'Double Cheese Burger';
  console.log(secondBurger.name ); // 'Double Cheese Burger';
  un solo oggeto in memoria


2 console.log(hamburger.ingredients[0]); // 'salad'
  console.log(secondBurger.ingredients[0]); // 'salad'
  solamente un oggetto


3 tre oggetti


4 creare una shallow copy con il destructuring visto che la funzione non verrebbe copiata
con nessun altro metodo
usare JSON.stringify visto che si tratta di un oggetto complesso per via di date